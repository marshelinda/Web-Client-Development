function calculateRectangleArea(length, width) {
    return length * width;
}

// Example usage:
const length = 5;
const width = 3;
const rectangleArea = calculateRectangleArea(length, width);
console.log(`Area of rectangle: ${rectangleArea}`); // Output: 15